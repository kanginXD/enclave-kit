# Copy to config.yml.
username: dev
password: changeme

# Required. OpenSSH public keys.
ssh_authorized_keys: []

# Guest disk size. Omit to keep the cloud image size.
disk_size: 64G

packages: []

# cursor, claude, tailscale
tools:
  - cursor
  - claude
  - tailscale

# lxqt
desktop: lxqt
